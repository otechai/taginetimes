# Hugo build output — generated by the deploy.yml workflow, never commit this.
# Committing/zipping this folder by hand is exactly how the localhost:1313
# baseURL bug shipped to production. Always let CI build it.
/public/

# Hugo runtime artifacts
/resources/
.hugo_build.lock

# OS / editor cruft
.DS_Store
Thumbs.db
